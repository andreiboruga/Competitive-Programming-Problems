#include <bits/stdc++.h>
using namespace std;

void solve()
{
    int n, x;
    cin>>n>>x;

    if(x == 1)
    {
        cout<<"-1\n";
        return;
    }

    vector<int> a(n);
    vector<int> dp(n);
    cin>>a[0];
    dp[0] = a[0];
    for(int i = 1; i < n; i++)
    {
        cin>>a[i];
        if()
    }
}

int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        solve();
    }

    return 0;
}
